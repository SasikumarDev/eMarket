export interface ModelContext {
}

export interface Login {
    email: string;
    Password: string;
}

export interface Category {
    CID: string;
    CDesc: string;
    CImgpath: string;
    CStatus: string;
}