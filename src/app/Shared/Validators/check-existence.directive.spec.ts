import { CheckExistenceDirective } from './check-existence.directive';

describe('CheckExistenceDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckExistenceDirective();
    expect(directive).toBeTruthy();
  });
});
